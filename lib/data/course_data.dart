class CourseData {
  const CourseData._();

  /// Total number of available lessons in the course.
  /// Used by the dashboard and achievements (like Sukatech Scholar) to calculate completion.
  static const int totalLessons = 6;
}
